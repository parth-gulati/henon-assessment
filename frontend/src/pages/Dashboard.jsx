import React from 'react';
import { Typography } from '@mui/material';

const Dashboard = () => {
  return (
    <div>
      <Typography variant="h4">Dashboard</Typography>
      {/* Timeline and event manager goes here */}
    </div>
  );
};

export default Dashboard;